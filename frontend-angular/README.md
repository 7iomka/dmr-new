# DMR Angular Migration Workspace

## Current phase
Shell/layout parity + routing foundation:
- legacy-like desktop sidebar and topbar
- dashboard desktop migration (v1)
- top-level routes with page stubs for phased migration

## Run
```bash
npm install
npm run start
```
